name: test

description: "Testing a simple nitric service"

resources:
  ingress:
    type: entrypoint
    routes:
      /:
        name: app

services:
  app:
    env:
      TEST: test
    dev:
      script: go run main.go
    container:
      docker:
        dockerfile: Dockerfile
        context: .
